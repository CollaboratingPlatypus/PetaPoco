name: Build and Test

on:
  push:
    branches:
      - 'feature/petapoco-v7'
#      - main
#  pull_requests:
#    branches:
#      - main

jobs:
  runner-job:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - uses: ./.github/workflows/workflow-build-and-test.yml
      - uses: actions/checkout@v2
